"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Check, Sparkles } from "lucide-react";
import { Purchases } from "@revenuecat/purchases-js";
import { useAuth } from "@/components/auth/AuthProvider";
import { PublicPage } from "@/components/public/PublicChrome";
import { StatusMessage } from "@/components/ui/status-message";

const plans=[{id:"monthly",name:"Monthly",price:"$29.99",detail:"per month"},{id:"six-month",name:"Six months",price:"$79.99",detail:"every 6 months",badge:"Best value"}] as const;
export default function SubscriptionPage(){const auth=useAuth();const router=useRouter();const [busy,setBusy]=useState("");const [message,setMessage]=useState("");async function buy(planId:string){setMessage("");if(!auth.user){router.push("/login?returnTo=/subscription");return;}const apiKey=process.env.NEXT_PUBLIC_REVENUECAT_WEB_API_KEY;if(!apiKey){setMessage("Subscriptions are not configured yet. Add NEXT_PUBLIC_REVENUECAT_WEB_API_KEY and the package identifiers described in .env.example.");return;}setBusy(planId);try{const purchases=Purchases.configure({apiKey,appUserId:auth.user.uid});const offerings=await purchases.getOfferings();const offering=offerings.current;if(!offering)throw new Error("No current RevenueCat offering is configured.");const wanted=planId==="monthly"?(process.env.NEXT_PUBLIC_REVENUECAT_MONTHLY_PACKAGE_ID||"$rc_monthly"):(process.env.NEXT_PUBLIC_REVENUECAT_SIX_MONTH_PACKAGE_ID||"six_month");const rcPackage=offering.availablePackages.find(p=>p.identifier===wanted);if(!rcPackage)throw new Error(`The ${planId} package is not available in the current RevenueCat offering.`);await purchases.purchase({rcPackage,customerEmail:auth.user.email||undefined});router.push("/dashboard");}catch(e){setMessage(e instanceof Error?e.message:"The purchase could not be completed. Please try again.");}finally{setBusy("");}}return <PublicPage><main className="mx-auto max-w-5xl px-5 py-16 lg:px-8"><div className="text-center"><p className="text-xs font-semibold uppercase tracking-[.16em] text-[var(--primary)]">Simple pricing</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Choose the plan that works for you</h1><p className="mx-auto mt-4 max-w-2xl text-[var(--muted-foreground)]">Access every professional mode, template, AI drafting workflow, history, and favorites.</p></div>{message&&<StatusMessage type="error" className="mx-auto mt-8 max-w-2xl" title="Subscription unavailable">{message}</StatusMessage>}<div className="mx-auto mt-10 grid max-w-3xl gap-5 md:grid-cols-2">{plans.map(plan=><article className={`relative rounded-[28px] border bg-[var(--card)] p-7 shadow-lg ${plan.badge?"border-[var(--primary)]":"border-[var(--border)]"}`} key={plan.id}>{plan.badge&&<span className="absolute right-5 top-5 rounded-full bg-[var(--primary-soft)] px-3 py-1 text-xs font-semibold text-[var(--primary)]">{plan.badge}</span>}<h2 className="text-lg font-semibold">{plan.name}</h2><p className="mt-5 text-4xl font-semibold tracking-tight">{plan.price}</p><p className="mt-1 text-sm text-[var(--muted-foreground)]">{plan.detail}</p><ul className="mt-7 space-y-3 text-sm">{["All professional modes","Unlimited clinical templates","Voice and typed input","History, favorites, and auto-save","Edit every AI-generated draft"].map(x=><li className="flex gap-2" key={x}><Check className="size-4 text-[var(--primary)]"/>{x}</li>)}</ul><button disabled={Boolean(busy)} onClick={()=>void buy(plan.id)} className="mt-8 h-12 w-full rounded-xl bg-[var(--primary)] font-semibold text-white disabled:opacity-60">{busy===plan.id?"Opening secure checkout…":"Choose this plan"}</button></article>)}</div><div className="mx-auto mt-8 flex max-w-2xl items-start gap-3 rounded-2xl bg-[var(--primary-soft)] p-5 text-sm"><Sparkles className="mt-0.5 size-5 shrink-0 text-[var(--primary)]"/><p><strong>Clinical responsibility stays with you.</strong> ShiftNote drafts documentation from the facts you provide. Review and attest every note before clinical use.</p></div></main></PublicPage>}

