import { SetupPreview } from '@/components/enterprise/SetupPreview';
export const metadata = { title: 'Enterprise Setup Preview', robots: { index: false, follow: false }, referrer: 'no-referrer' as const };
export default function Page() { return <SetupPreview />; }
