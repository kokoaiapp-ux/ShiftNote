# ShiftNote Enterprise

The UI-only preview has been replaced by the authenticated Enterprise backend.

See [Enterprise backend and rollout](enterprise-backend.md) for the schema, access controls, SMTP configuration, initial administrator provisioning, and required validation before deployment.

Enterprise marketing and demo requests are public. Organization workspaces require an approved setup link and Enterprise administrator membership. The internal `/admin` portal requires a separately provisioned KOKO LABS role. No fictional organization or activity data is used.
