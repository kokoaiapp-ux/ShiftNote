"use client";
import { useState } from "react";
import { buttonClass, fieldClass, Panel } from "./ui";
import { organization } from "@/lib/enterprise/demo-data";
export function SettingsPreview() {
  const [saved, setSaved] = useState(false);
  return <form onSubmit={event => { event.preventDefault(); setSaved(true); }} onChange={() => setSaved(false)} className="max-w-3xl"><Panel><h2 className="text-lg font-semibold">Organization preferences</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">Organization display name<input required name="name" defaultValue={organization.name} className={fieldClass} /></label><label className="text-sm font-medium">Country<input required name="country" defaultValue="United States" className={fieldClass} /></label><label className="text-sm font-medium">Time zone<select name="timezone" defaultValue="America/Los_Angeles" className={fieldClass}>{["America/Los_Angeles","America/Denver","America/Chicago","America/New_York","Europe/London","UTC"].map(zone => <option key={zone}>{zone}</option>)}</select></label><label className="text-sm font-medium">Reporting period<select name="period" className={fieldClass} defaultValue="Monthly"><option>Monthly</option><option>Weekly</option><option>Quarterly</option></select></label></div><label className="mt-6 flex items-center gap-3 text-sm"><input type="checkbox" name="summary" defaultChecked className="size-4 accent-[var(--primary)]" />Include estimated time saved in reports</label><p className="mt-6 text-xs leading-6 text-[var(--muted-foreground)]">Changes apply to this form only and reset when you leave or refresh. They do not affect your Professional settings.</p><button className={`${buttonClass} mt-5`} type="submit">Save preview</button>{saved && <p role="status" className="mt-4 text-sm text-[var(--primary)]">Preview updated. No organization data was stored.</p>}</Panel></form>;
}
