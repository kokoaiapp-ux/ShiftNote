import { monthlyUsage, organization } from "@/lib/enterprise/demo-data";
import { Panel } from "./ui";

export function AnalyticsOverview() {
  const metrics = [["Active Clinicians", String(organization.activeClinicians), "of 248 sample clinicians"], ["Facilities", "4", "Across the sample organization"], ["Departments", "12", "Across four care settings"], ["Notes Generated", "4,280", "Sample September activity"], ["Estimated Time Saved", "356.7 hours", "Illustrative estimate: 5 min per note"], ["AI Utilization", "75%", "186 of 248 sample clinicians"]];
  return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{metrics.map(([label,value,hint]) => <Panel key={label}><p className="text-sm text-[var(--muted-foreground)]">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-[var(--muted-foreground)]">{hint}</p></Panel>)}</div><Panel className="mt-5"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="text-lg font-semibold">Monthly Usage</h2><p className="text-xs text-[var(--muted-foreground)]">Illustrative documentation volume</p></div><div className="mt-6 space-y-4">{monthlyUsage.map(item => <div key={item.month} className="grid grid-cols-[5rem_1fr_3rem] items-center gap-3 text-xs"><span>{item.month}</span><div aria-hidden="true" className="h-3 overflow-hidden rounded-full bg-[var(--muted)]"><div className="h-full rounded-full bg-[var(--primary)]" style={{ width: `${item.notes / 4280 * 100}%` }} /></div><span className="text-right tabular-nums">{item.notes.toLocaleString("en-US")}</span></div>)}</div></Panel></>;
}
